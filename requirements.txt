# Web server
fastapi>=0.110
uvicorn>=0.24
pydantic>=2.6
python-multipart>=0.0.9

# HTTP & image
requests>=2.31
Pillow>=10.0

# Background removal (CPU, ONNX)
rembg[cpu]>=2.0

# LLM client
openai>=1.0

# SVG validation (lxml only; cairosvg/svglib removed to avoid native cairo dependency)
lxml>=4.9
